<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PizzaApp.CartPage"
             Title="Cart"
             x:DataType ="viewModels:CartPageViewModel"
             xmlns:viewModels="clr-namespace:PizzaApp.ViewModels"
              xmlns:models="clr-namespace:PizzaApp.Models">
    <StackLayout Margin="25">
        <Label Text="Your Items!"
                VerticalOptions="Center" 
                HorizontalOptions="Center"
               FontSize="Large"/>
        
        <CollectionView ItemsSource="{Binding CartCollection}" Margin="50">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>                
                <DataTemplate x:DataType="models:PizzaMenuItem">
                    <Grid ColumnDefinitions="4*,*" HorizontalOptions="Center">
                        <Label Text="{Binding Name}" HorizontalTextAlignment="Start" />
                        <Label Text="{Binding Price}" HorizontalTextAlignment="End" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
